# Socks-VPS v1.2.0 历史交接

历史交接：以下行为、发布与未完成项均对应文中所列版本和当时验证。现行约束见[根AGENTS](../../../AGENTS.md)，当前状态见[项目待办](../../todo.md)，版本导航见[时间线](../timeline.md)。

更新时间：2026-07-28

## 当时发布

- 仓库：`https://github.com/mqfut123/Socks-VPS`
- 当时公开发布：`v1.2.0`
- Git 基线：`6864b669138fb087628e2fdd749fa61fba794825`
- Release：`https://github.com/mqfut123/Socks-VPS/releases/tag/v1.2.0`
- 公开稳定入口：`https://github.com/mqfut123/Socks-VPS/releases/latest/download/install.sh`
- 项目许可：MIT，项目许可正文位于 `LICENSE`
- 发布包：Linux `amd64`、Linux `arm64`
- IPdeny：CN IPv4 aggregated 数据随仓库和同一版本发布包交付

`v1.2.0` 保持现有 Go SOCKS 协议、配置 schema、公开命令和退出码，完成
中文交互、TTY 彩色阶段提示、永久删除生命周期、依赖安装确认、真实占用
感知的自动端口选择和“清理日志”入口。持久备份、快照、恢复命令和隐藏
`--restore` 接口已经删除；失败恢复只存在于当前调用内。

## 当时产品边界

- 独立 Go 服务，不使用 Xray 运行时。
- 一个进程读取 `/etc/socks-vps/instances/*.json`，运行多个独立 SOCKS
  配置。
- 监听 `tcp4` 和 `0.0.0.0:<port>`。
- 只支持 SOCKS5、RFC 1929 用户名/密码认证和 TCP `CONNECT`。
- 域名只解析 A 记录，出站只使用 `tcp4`。
- 拒绝 IPv6、UDP、BIND、私网、本机、链路本地、云元数据和其他
  IPv4 特殊用途目标。
- CN 来源由 `table ip socks_vps` 在 TCP 握手前丢弃；Go 服务不重复判断
  CN 来源。
- 未命中的数据包继续经过主机已有 nftables、UFW、firewalld 和云防火墙，
  项目不添加全局 accept。
- WARP VPS Manager 的服务、网卡、路由、配置和 `table inet warp_vps`
  始终是外部资源。

## 权威状态与资源

- 配置：`/etc/socks-vps/instances/*.json`
- 当前版本链接：`/usr/local/lib/socks-vps/current`
- 版本目录：`/usr/local/lib/socks-vps/releases/<version>`
- 程序入口：`/usr/local/bin/socks-vps`，普通文件并与当前 release 内容一致
- 管理入口：`/usr/local/bin/socks-vpsctl`
- 服务：`socks-vps.service`、`socks-vps-firewall.service`
- 防火墙：`table ip socks_vps`
- 所有权标记：`cn_ipv4` set 注释 `Socks-VPS managed CN IPv4 set`

每个端口、凭据和大陆来源设置只以对应实例配置为权威。手动端口冲突时
显示占用者并停止；自动端口以服务真实 bind 结果为准，只有新选端口发生
竞争时重选一次。

`/var/backups/socks-vps/` 不再是当前状态来源。成功的特权生命周期操作或
“清理日志”会删除旧版持久备份、非当前 release、`.migrated-*`、
`.bind-failed-*` 及已知 pending/temp 残留。

## 当时生命周期

- 安装、更新和重装只在开始系统写入时执行完整安装包校验；实例管理和卸载
  只检查当前动作需要的运行资源和所有权。
- 缺少 `nft`、`ip` 或 `ss` 时先列出依赖，并询问
  `是否安装以上依赖？[Y/n]`；回车或 `y/Y` 继续，`n/N` 在 sudo 和包管理器
  写入前取消。APT 只安装缺少的 `nftables`、`iproute2`，DNF/YUM 只安装
  缺少的 `nftables`、`iproute`。
- `add`、`credentials`、`remove`、`update` 和 `reinstall` 仅在同一次
  操作内暂存原值；失败时恢复，成功时立即清理，不保留 snapshot、marker
  或 `restore` 命令。
- `remove` 永久删除目标配置，最后一个实例仍必须通过 `uninstall` 删除。
- 重装永久替换全部实例配置，并在首次写入前明确确认。
- 卸载在首次删除前确认当前 release、公开二进制、当前版本应有的管理命令
  和 unit 的精确所有权；历史 v1.0.x 本身没有管理命令，只按其 VERSION
  契约接受缺失。随后停止并禁用服务、撤销自有监听和 nftables 表，永久
  删除全部项目文件、历史备份、运行目录以及专用用户和组。
- 共享系统包、外部同名 nftables 表和所有 WARP VPS Manager 资源保持不变。
- 防火墙 `remove` 不读取实例配置或 IPdeny 数据；配置损坏不能阻止删除
  已确认归属本项目的表。
- `status` 是只读综合检查，覆盖配置、systemd、监听归属、认证和所需
  防火墙状态，不在检查中启用服务。
- “清理日志”只删除 Socks-VPS 自有的旧备份、历史 release、事务目录、
  运行时 pending/temp 和已知迁移/绑定失败残留；不删除共享 journal，不
  重启服务，也不修改当前配置。

## v1.2.0 验证

最终提交和发布输入已通过：

- Bash 语法；Go test、vet、race、gofmt。
- 账户、契约、防火墙、生命周期、监听归属、事务恢复和清理回归。
- `amd64`、`arm64` 候选包、最终包、成员清单、依赖锁定、源码清单、
  公开 bootstrap、精确二进制重建和可复现构建。
- Ubuntu 24.04.4 arm64 与 AlmaLinux 9.8 arm64 的完整新生命周期实测。
- AlmaLinux SELinux Enforcing 下公开二进制为 `bin_t`，systemd 服务真实
  RFC 1929 认证 SOCKS 公网出站成功。
- 两套系统均覆盖安装、自动端口真实 bind 竞争只重选一次、手动冲突、
  重启、模拟 CN/非 CN 防火墙、损坏/缺失配置后的自有表删除、外部同名表
  保留、失败更新当次恢复、凭据轮换、重装、最后实例拒绝单独删除、所有权
  篡改时卸载在停止服务前拒绝，以及损坏配置下永久卸载。
- 两套系统均完成正式 `v1.1.0` 到 `v1.2.0` 升级，保留端口、凭据和
  `allow_cn`；旧公开 symlink 转为普通文件，AlmaLinux 保持 `bin_t`。
- APT 与 DNF 的依赖确认均验证了 `n` 无写入取消、回车只安装缺失依赖。
- GitHub `latest/download/install.sh` 在全新 Ubuntu 上完成公开安装、
  非 TTY 无 ANSI、综合状态检查和永久卸载。

最终发布文件 SHA-256：

- `install.sh`：`b633fe3d6ee78ffd4cff2d85fe26a34ee92e8b66a3b1a4c45673f4c93d7da901`
- `socks-vps-v1.2.0-linux-amd64.tar.gz`：
  `23b00855ad5f60c19bb61730f47f5481bc7fc2c7574822221dcc89429e1f436b`
- `socks-vps-v1.2.0-linux-arm64.tar.gz`：
  `2d3655c2cf597bac2907e6865b96466de8d2a1b617383d85b8cf05b0901304cc`

## 当时交互与生命周期约定

- 交互终端显示青色信息、黄色进行中、绿色成功和红色失败；非 TTY、管道
  和日志只输出普通文本。
- 环境检查、安装包校验和服务自检按阶段合并；成功不逐个打印文件名，失败
  保留当前诊断。
- 公网 IPv4 查询失败时要求用户填写，不使用可能为私网的路由源地址。
- 凭据、测试机地址和 SSH 信息不得写入 Git、handoff、日志或发布包。

## 当时尚未完成的独立验收

- `amd64` 的 v1.2.0 实机运行；本轮只完成该架构的精确重建和包级门禁。
- 真实 CN/GFW 来源。
- active firewalld。
- 与 WARP VPS Manager 同机安装、更新、重启和卸载。
