# 全局 AI 协作规则

标准日期：2026-09-11。

本文件在所有受管项目中必须逐字一致，禁止单仓库自定义。修改通用规则时必须同步全部受管项目并检查一致性；项目特定规则、运行约定和待办只写在 `cowork/`。用户当前明确指令优先于本文件和项目文档，已有授权在任务内持续有效。

## 开始工作

1. 先读 `cowork/background.md` 的项目背景、仓库边界、文档索引和 Outline 状态，再读 `cowork/todo.md`。
2. 向当前执行人列出本项目未完成待办的编号、状态和下一步，标明本次请求对应的事项；已完成项不重新列为待做。清单用于同步工作，不要求用户对已授权的任务再次确认。
3. 读 `cowork/rules.md` 及其指向的适用子目录规则，再读与当前任务有关的设计、操作、踩坑、时间线和错误记录。
4. 开始修改前核对当前源码、配置、调用方、测试、Git状态、分支和远端。文档或问题描述是待核实的依据，当前实现与真实运行结果决定具体改动。

## 目录与权威来源

- `AGENTS.md`：全局一致的协作和文档规则；仅做全体受管项目同步更新。
- `cowork/background.md`：项目用途、背景、结构、仓库、当前阶段与文档索引。
- `cowork/todo.md`：本仓库代码、运维和交付待办的唯一状态入口；多模块按模块分节。
- `cowork/rules.md`：项目专属约定、命令、测试与部署规则，以及适用子目录规则索引。
- `cowork/docs/`：设计、运行维护、踩坑和时间线；优先迁入或引用现有资料，不建立重复的现行说明。
- `cowork/error/`：本仓库的错误记录。错误跟随所属项目，不发到另一个统一错误仓库。

`cowork/` 顶层固定为 `todo.md`、`rules.md`、`background.md` 三个文件，以及 `docs/`、`error/` 两个目录。项目需要的更细分类放进这两个子目录，保持所有项目的协作结构一致。根 `README.md` 保留正常项目入口；被构建或发布直接引用的资料可保持原路径，由 `cowork/docs/README.md` 索引其唯一权威位置。

源码、运行必需配置、测试和项目协作 Markdown 在本仓库维护。background、todo、错误记录、时间线之间使用仓库内相对链接；跨仓库使用已核实的 Git 链接和具体提交或 Issue。一个事实只有一个当前权威位置，其他文档引用该位置。

## Outline

Outline 用于同步项目介绍、阶段进度、安全基线及其他需要团队共同阅读的资料，默认只读。具体入口与上线状态放在 `cowork/background.md` 中。

- 标为“未上线，暂无需阅读和更新”时，按本仓库资料工作，不把 Outline 当作 blocker，不猜测 URL 或反复尝试连接。
- 上线后可读取与任务相关的页面；代码层面的小改动和日常待办首先更新 GitHub 中的 Markdown。
- 仅在信息准确性需要更新且当前任务已获相应授权时，对对应 Outline 页面做最小更新，例如大版本发布、部署方式或安全基线发生重要变化。先准备可审阅的差异，再在授权范围内写入并回读结果。
- 不因文件同步方便而覆盖整页、重写无关内容或维持两份独立编辑的工程待办。记录同步页面、日期及关联提交。

## 实现原则

从实际需求和当前证据出发。关键目标或影响范围不明时先讨论；现有实现已满足需求时不改代码。优先复用现有实现、标准库和已安装依赖，在最早的共同边界完成最小正确改动。

保持公开接口、数据格式、schema和项目惯例。每项改动都要对应本次目标、已复现问题或必要验证，不顺手重构、改名、格式化相邻代码或增加未来扩展点。没有明确需要时，不新增生产依赖、服务、状态、兼容层、重试或恢复机制。

先区分只读、原样复用、无操作与真正写入，再实施该分支需要的检查。仅为当前信任边界或实际财务、安全、外部写入保护明确不变量；保护写入的条件不阻止合法查看和无操作。校验误伤主路径时先判断它是否必要，不围绕错误校验增加绕过或回退。

接口、命令、字段和配置必须来自官方契约、锁定版本源码或实际环境；不能核实时说明缺口。已知限制返回具体原因和可执行下一步，详细诊断进入项目现有日志。

## 维护待办、错误与时间线

- 待办先匹配已有编号，避免同义重复；记录事项、状态、负责人或执行角色、下一步和完成依据。发现本次范围外的问题时登记，不擅自实施。
- “已发送、已提交、已上传、已合并”只完成对应阶段；等待回复、生产字节核验、真实业务验收等后续单独写明。用户明确确认完成时记录日期、范围和依据，归入已完成，不继续催办同一动作。
- 错误先查本仓库 `cowork/error/` 和已有 Issue。记录发生时间、环境或版本、现象、复现与影响、证据、判断、处理和验证；相关待办通过同一编号或链接关联，错误正文不另维护第二套待办状态。
- 只将经证据支持的问题写为缺陷；未验证的猜测标明待核。纯样式调整、正常无操作和预期业务拒绝不制造错误记录。
- 时间线在 `cowork/docs/timeline.md` 或其索引的既有权威文件中按事件发生日期记录重要行为变化、发布、迁移、事故、修复和关键决定，关联提交、错误、待办及验证层次。追加后续事件，不改写过去发生过什么；纠正旧判断时保留纠正说明。
- `cowork/docs/pitfalls.md` 及其索引的专题踩坑文档提炼可复用的触发条件、原因、处理和验证；操作手册写现行做法，历史过程放时间线，不重复堆积聊天记录。

## 文档随改动更新

行为、接口、配置、部署或操作方式变化时，更新对应项目文档。修复问题时关联错误、todo和时间线；仅调整措辞等小改动不机械新增文档。

移动文件前列出受影响路径和去向，检查引用、脚本、测试与发布清单，再同步更新链接。历史技术资料可按目录索引保留；不要将密钥、会话、个人资料、客户原始记录、财务材料、运行数据或无关内部信息混入协作文档和公开仓库。

## 验证与交付

执行与改动匹配的检查，覆盖真实正常路径和需要拒绝的写入路径；不为制造通过而关闭或削弱现有检查。区分源码扫描、模拟、单元测试、本地运行、测试环境和生产验收，未验证部分直接写明。

完成前审查最终diff、Git状态、分支与远端，确认没有夹带他人或无关修改。范围明确且验证通过时按项目既有流程提交推送；有自动部署链时先确认文档或规则变更会触发什么。部署、资源变更和对外发送按本次实际授权执行。

Git提交与网站发布分别管理。含真实秘密的`.env`、私钥、Token、凭据和运行数据不进入Git；无秘密的`.env.example`等示例可按项目规则提交。点前缀不决定能否提交或发布，`.well-known/`等运行必需目录按实际用途明确纳入。

协作资料在Git仓库按仓库权限共享，不属于网站公开内容。发布前检查构建、打包、同步和静态服务根：`cowork/`、`AGENTS.md`及项目内部规则不得进入公开网站产物或可通过网站URL访问。优先沿用明确的运行文件/构建输出目录；不能用robots.txt、隐藏链接或仅跳过一次workflow代替发布隔离。

交付说明包含：完成了什么、涉及哪些文件或文档、如何验证、提交与推送结果、仍需处理的事项。Agent交接使用同一组信息：当前目标、已核实事实、完成内容、验证结果、未决项和下一步。

## 新项目接入

创建新项目时，先复制本统一 `AGENTS.md`，建立相同的 `cowork/` 三文件、`docs/` 和 `error/`，填入真实项目背景、规则、待办及文档索引。补齐 `docs/README.md`、`docs/pitfalls.md`、`docs/timeline.md` 和 `error/README.md`，已有等价资料时索引或迁入，不复制第二份状态。Outline未上线时使用统一状态文字；上线与授权同步由知识库项目的待办跟踪。记录新仓库与标准日期并将其加入全局更新范围。

## 操作与沟通

- 与用户交流称“您”。直接陈述事实、决定和下一步；出错时承认并纠正，不反复辩解。不添加无关免责声明。
- 对外消息、工单、邮件及附件须有相应授权，只发送解决当前问题所必需的信息；不因整理任务自动获得发送、采购、改账或生产变更权限。
- Chrome仅在本次任务明确授权后使用，并新开任务专用独立窗口；不读取或接管用户既有窗口、标签、历史或会话。
- 只在当前工作范围和已明确授权的项目路径内操作。批量移动、覆盖或重生成前说明路径和影响。
- 禁止永久删除文件；删除使用系统安全回收。保留登录态、密钥、运行状态、未核实数据和唯一交付物；清理可重建产物前核对用途与可恢复性。
