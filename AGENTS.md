# Socks-VPS 工作规则

## 项目关系

- Socks-VPS 是独立项目，与 WARP VPS Manager 没有代码、版本、发布或配置继承关系。
- 两个项目可能安装在同一台 Linux VPS。设计安装、运行、更新和卸载流程时，必须把共存作为真实环境要求。
- WARP VPS Manager 的服务、端口、网卡、路由、nftables 表、配置和状态均视为外部资源；Socks-VPS 不得接管、覆盖、停止、删除或借用其状态。
- “部署机器相同”只用于识别共存约束，不能作为复制另一个项目实现、依赖或系统结论的依据。

## 开始工作前

1. 阅读 `reference/README.md`。
2. 阅读当前需求相关的实现、调用方、测试和文档。
3. 搜索项目中已有能力，并核对依赖的官方契约与当前锁定版本。
4. 目标、协议、信任边界或系统影响范围不清楚时，先与用户确认，不猜测 Socks-VPS 的产品形态。

## 实现原则

- 选择最短的正确路径，优先复用标准库、系统能力和已安装依赖。
- 只修改当前目标需要的代码，不添加未来扩展点、兼容层、影子状态、恢复链或第二套判断。
- 一个事实只保留一个权威来源，在最早的共同边界完成一次决策。
- 维护公开命令、配置、数据格式和运行行为；需要改变契约时先获得确认。
- 网络、服务和文件资源必须有清晰所有权。安装前识别冲突，卸载时只处理 Socks-VPS 自己创建或明确接管的资源。
- 遵循 `reference/README.md` 中的系统适配、失败行为、安装体验、共存和验收偏好。

## 校验与交付

- 只在不可信输入、官方契约或即将发生的特权、网络、安全及外部写操作边界进行必要校验。
- 本地 shell、mock 或单元测试不能替代 Linux VPS 上的 systemd、端口、路由、防火墙和真实流量验收。
- 修改后运行与风险相称的检查，审查最终 diff、Git 状态、当前分支和远端。
- 文档先写用户获得的结果和最短使用入口，再写实现与维护细节；不使用自我澄清、兜底辩解或重复免责声明。
