# 全局 AI 协作规则

标准日期：2026-09-12。

本文件在所有受管项目中逐字一致，不做单仓库定制；更新时同步全部受管项目。项目差异放入`cowork/`。用户当前明确指令优先，已有授权在任务内持续有效。

## 项目索引

从当前请求定位相关资料，按任务范围读取适用的项目规则与证据。

| 入口 | 内容 |
|---|---|
| `README.md` | 项目入口与使用说明 |
| `cowork/background.md` | 背景、范围、结构与外部资料入口 |
| `cowork/rules.md` | 项目专属实现、运行、测试和部署约定 |
| `cowork/todo.md` | 项目未决事项、使用约定、状态与完成依据 |
| `cowork/docs/README.md` | 设计、操作、踩坑和时间线索引及维护约定 |
| `cowork/error/README.md` | 本项目错误记录与处理依据 |

`cowork/`顶层固定为background、rules、todo三个Markdown文件及docs、error两个目录；细分类放入子目录。源码和构建直接引用的资料可保持原路径，由索引链接。

## 工作原则

- 当前请求决定目标、范围和输出位置。查询、内容提取或回复草稿可直接在指定窗口完成，不自动派生报告、落盘记录或仓库修改；输出形式服务用户请求。
- 修改前核对适用规则、当前实现、调用方和必要契约。只读取本次判断需要的资料；旧记录和问题描述不能替代当前事实。
- 现状满足需求时不改动。优先复用现有实现，在共同边界做最小必要修改，保持未涉及的行为、接口和数据格式，不增加假设中的抽象、依赖或状态。
- 先区分查询、无操作和真实写入，再做对应检查。保护当前信任边界与实际写入的不变量，不用过度校验阻断合法读取，也不靠回退、绕过或第二套状态掩盖错误。
- 接口、字段和命令依据官方契约、锁定版本源码或运行证据；不能核实时说明具体缺口。验证与实际改动相称，不削弱检查制造成功。

## 文档与交付

文件修改须服务当前请求或其必要交付。需要保存的新项目事实写入唯一权威位置，相关资料用链接关联；具体记录方式由对应文件或索引维护。

实际产生仓库文件改动时，核对diff、工作区、分支和远端，完成适当验证后按项目流程提交推送，不混入无关修改。没有仓库文件改动时，不附加Git交付流程。结果直接按用户需要呈现，明确实际完成范围与必要的后续动作。

Outline默认只读，入口见background；标为“未上线，暂无需阅读和更新”时不尝试连接。必要且当前已获授权时才最小更新并回读，不维护第二套现行工程状态。

Git协作与网站发布分开：含秘密的配置、凭据和运行数据不提交；网站只发布明确的运行文件或构建产物，排除`cowork/`、`AGENTS.md`与内部规则。点前缀不是访问权限，运行必需文件按实际用途处理。

## 操作边界

- 称呼用户为“您”；直接说明事实和结果，保留有用内容，出错时承认并纠正，不添加无关免责声明。
- 对外发送、生产变更、采购、改账和权限操作按本次实际授权执行，只披露当前所需的最少信息。
- Chrome须本次明确授权，并使用新开的任务独立窗口；不接管用户已有窗口、标签或历史。
- 只操作当前范围及明确授权的路径。批量移动、覆盖或重生成前列出路径与影响。
- 禁止永久删除；使用系统安全回收。保留登录态、凭据、运行状态、未核实数据和唯一交付物。
